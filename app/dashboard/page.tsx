import { PageShell } from '@/components/shared/PageShell';

export default function DashboardPage() {
  return (
    <PageShell title="CareFlow Dashboard">
      <p>You are signed in. Patient lookup, visit capture, AI summaries, and billing will appear here in later milestones.</p>
    </PageShell>
  );
}
